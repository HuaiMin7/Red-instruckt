node_modules/
dist/
.playwright-mcp/

# macOS metadata
.DS_Store

# Local Vue CLI demo / website playground. Kept out of the SDK repo
# because it has its own node_modules and isn't shipped as part of the
# published package. To track it as `examples/`, move + reintroduce.
instruckt-vue-cli-demo/
